export enum ScreenOptions {
    MAIN_MENU,
    SINGLE_PLAYER_MENU,
    CHARACTER_SELECT,
    GAME,
}

export enum CharacterOptions {
    KNIGHT = "knight",
    KOBOLD = "kobold"
}

export enum Attributes {
    HEALTH = "health",
    SPEED = "speed",
}

export enum PlayerStates {
    WALKING,
    JUMPING,
    FALLING,
    STANDING
}
